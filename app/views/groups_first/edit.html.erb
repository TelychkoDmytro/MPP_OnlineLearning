<h1><%= @group.name %></h1>


<%= form_with(model: @group, local: true) do |form| %>

	<div class="field">
		<%= form.label :name %>
		<%= form.text_field :name %>
	</div>
	
	<div class="actions">
		<%= form.submit "Update" %>
	</div>

<% end %>

<%= link_to 'Back', groups_path %>