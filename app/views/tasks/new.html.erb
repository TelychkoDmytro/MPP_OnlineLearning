<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Task</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
    }
    .form-container {
      max-width: 900px;
      margin: 50px auto;
      background: #fff;
      padding: 20px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      border-radius: 10px;
    }
    .form-header {
      text-align: center;
      margin-bottom: 20px;
    }
    .form-header h1 {
      margin: 0;
      font-size: 28px;
      color: #333;
    }
    .form-horizontal .form-group {
      margin-bottom: 15px;
    }
    .btn-primary {
      background-color: #FFA500;
      border-color: #FFA500;
    }
    .btn-primary:hover {
      background-color: #FF8C00;
      border-color: #FF8C00;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <div class="form-header">
      <h1>New Task</h1>
      <p>Find me in app/views/tasks/new.html.erb</p>
    </div>

    <%= form_with(model: [@subject, @task], url: subject_tasks_path, local: true, html: { class: 'form-horizontal' }) do |form| %>
      <% if @task.errors.any? %>
        <div id="error_explanation" class="alert alert-danger">
          <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>
          <ul>
            <% @task.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form.hidden_field :subject_id, value: params[:subject_id] %>

      <div class="form-group row">
        <%= form.label :title, class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= form.text_field :title, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :description, class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= form.text_field :description, class: 'form-control' %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :group_ids, "Groups", class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= form.collection_select :group_ids, @subject.groups, :id, :name, { prompt: 'Select groups'}, { multiple: true, class: 'form-control' } %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :max_score, class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= form.number_field :max_score, class: 'form-control' %>
        </div>
      </div>
      
      <div class="form-group row">
        <div class="col-sm-offset-2 col-sm-10">
          <%= form.submit 'Create Task', class: 'btn btn-primary' %>
          <%= link_to 'Back', subject_tasks_path(params[:subject_id]), class: 'btn btn-secondary' %>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>
