<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @task.title %></title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 900px;
      margin: 50px auto;
      background: #fff;
      padding: 20px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      border-radius: 10px;
    }
    .header {
      text-align: center;
      margin-bottom: 20px;
    }
    .header h1 {
      margin: 0;
      font-size: 28px;
      color: #333;
    }
    .task-details {
      background-color: #fff8e1;
      padding: 20px;
      border-radius: 5px;
    }
    .task-details h2 {
      color: #333;
      font-size: 24px;
      margin-top: 0;
    }
    .task-details p {
      font-size: 18px;
      color: #555;
    }
    .task-details .task-metadata {
      margin-top: 20px;
    }
    .task-metadata p {
      margin: 5px 0;
      font-size: 16px;
    }
    .btn-primary {
      background-color: #FFA500;
      border-color: #FFA500;
    }
    .btn-primary:hover {
      background-color: #FF8C00;
      border-color: #FF8C00;
    }
    .student-scores {
      margin-top: 30px;
    }
    .student-scores h2 {
      font-size: 24px;
      color: #333;
      margin-bottom: 20px;
    }
    .student-scores table {
      width: 100%;
    }
    .student-scores table th, .student-scores table td {
      padding: 10px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Task Details</h1>
      <p>Find me in app/views/tasks/show.html.erb</p>
    </div>

    <div class="task-details">
      <h2><%= @task.title %></h2>
      <p><%= @task.description %></p>

      <div class="task-metadata">
        <p><strong>Subject:</strong> <%= @task.subject.name %></p>
        <p><strong>Max Score:</strong> <%= @task.max_score %></p>
        <p><strong>Groups:</strong></p>
        <ul>
          <% @task.groups.each do |group| %>
            <li><%= group.name %></li>
          <% end %>
        </ul>
      </div>
    </div>

    <% if teacher_signed_in? %>
    <div class="student-scores">
      <h2>Student Scores</h2>
      <table class="table">
        <thead>
          <tr>
            <th>Student</th>
            <th>Score</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @task.student_task_scores.each do |student_task_score| %>
            <tr>
              <td><%= student_task_score.student.full_name %></td>
              <td><%= student_task_score.score %></td>
              <td>
                <%= link_to 'Edit', edit_subject_task_student_task_score_path(@task.subject, @task, student_task_score), class: 'btn btn-primary btn-sm' %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <% elsif student_signed_in? %>
      <% @task.student_task_scores.each do |student_task_score| %>
            <% if student_task_score.student.id == current_student.id %>
            <%= "Your score is #{student_task_score.score}" %>
            <% end %>
          <% end %>
    <% end %>

    <div class="mt-4">
      <%= link_to 'Back to Tasks', subject_tasks_path(@task.subject), class: 'btn btn-secondary' %>
    </div>
  </div>
</body>
</html>
