<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Opt Out of Subjects</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      background-color: #f4f4f4;
    }
    h1 {
      color: #333;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .col-form-label {
      font-weight: bold;
    }
    .form-control {
      width: 100%;
      padding: 10px;
      box-sizing: border-box;
      border-radius: 5px;
      border: 1px solid #ccc;
      font-size: 1em;
    }
    .row {
      display: flex;
      align-items: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Opt Out of Subjects</h1>
    <%= form_with(model: @student, url: update_opt_out_subjects_student_path, method: :post) do |form| %>
      <div class="form-group row">
        <%= form.label :subject_ids, "Subjects", class: 'col-sm-2 col-form-label' %>
        <div class="col-sm-10">
          <%= form.collection_select :subject_ids, @subjects, :id, :name, { prompt: false }, { multiple: true, class: 'form-control', size: 10 } %>
        </div>
      </div>

      <div class="form-group row">
        <div class="col-sm-10 offset-sm-2">
          <%= form.submit "Update Opt-Out Preferences", class: 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>
